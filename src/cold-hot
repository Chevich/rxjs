Observable

Cold: as playlist
* Нет значений без подписки (ленивы)
* Каждая подписка - свой независимый поток событий с начала (идентичный набор событий)

Hot: as radio
* Полученные события зависят от момента подписки
* Все подписчики получают одно и то же событие


of(), from(), timer(), interval()
fromPromise(), fromEvent()